[project]
name = "Udatracker"
version = "0.1.0"
description = "Udatracker is an order management system"
readme = "README.md"
requires-python = ">=3.11.9"
dependencies = [
    "flask==3.1.2"
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
